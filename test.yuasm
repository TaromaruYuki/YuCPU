.main start

main:
    psh 3
    psh 2
    psh 1

    jsr add3

    pop
    pop
    pop

    hlt

add3:
    psh rbp
    mov rbp, rsp

    sub rbp, 6
    ld r1, rbp

    sub rbp, 2
    ld r2, rbp

    sub rbp, 2
    ld r3, rbp

    add rbp, 10

    add r1, r2
    add r1, r3

    pop rbp

    ret
